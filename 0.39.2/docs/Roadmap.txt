# MAYBE ROADMAP (very unstable ;-) ):

# 0.39.2
# Man page for System, File !
# Stacktrace --> private !
# -Man.generator (use OODIR for manpath) !
# -System.sysdate if bash4.2 -> printf with %(fmt)T specifier -->strftime-like formating !
# -File: getLine, setLine, deleteLine, getLines, setLines, deleteteLines !
# -man oobash -->ReadMe.txt content !
# -use of System.out.print in generator DONE
# -i18n for "Sorry, no passwordless authentication possible with SshHost '$name' and user '$user'" DONE
#
# ssh/host ideas:
# remote2localCopy, local2remoteCopy remote2remoteCopy  --> copy
# remote2lcalDiff, local2remoteDiff, remote2remoteDiff  --> diff
# remoteCmd isAlive                                     --> command isAlive
# ssh -p 22 -q -t -o "BatchMode yes" $user@$machine whoami
# (-t only in interacive bash ?)
#
# -Process Class ?
# -Analyze log4oobash.properties ?
# -$Class.getAll functions if bash4.2 (declare global arrays) ?
# -possibility to check if argument is of type array ?
# -File.copy File.createSymLink ?
# -set own TMPDIR ?
# -quote String function ?
# -Generate class file directly into myClasses package structure ?
# -Decorator.help + man page ?
# -http://www.javamex.com/tutorials/threads/priority_what.shtml mapping to nice for threads Process.setPriority ?
# -check where gnu date is really neccessary/alternatives ?
# -not chained Exceptions, but chained return values ?


# 0.39.3
# -some addon methods for host, file, string ?

# 0.39.4
# url decode/encode ?

# 0.40
# Assert 
# + Tests (execute modules to test --- python style)

# 0.50
# Logging

# 0.60
# Use of /dev/tcp and /dev/udp
# for network sockets

# 0.70 
# Wrapper for some GUI libs ?

# 0.80
# Wrapper for some version control systems ?
