# MAYBE ROADMAP (very unstable ;-) ):

# 0.39.5
# -url decode/encode
# -some more Process functions:__getThreads, __children
# -setLine
# -File.createTemporaryDir, File.createTempDir (without suffix...)
# -Ssh: copy

   ########
   # DIFF #
   ########
   Not happy about the status quo: more thinking about possible diff methods...
   if arg is directory:
   -Recursively compare any subdirectories found, instead of diffing the results of two find commands ?
   --> new diffFileTree method using find ?
   -ignoreCase should be implemented in diff methods.

   ########
   # GREP #
   ########
   ideas for remoteGrep:
   -grep, grepIgnoreCase, grepInvertMatch (all three methods: optional: lineNumber, default: true)
   -grep default options:
   -rH --colour=never -n
   -grepCount ?

File: longestLine, shortestLine ?

-some test with base64Decode/Encode again...

-use of __isInteger for integer args

-remove version number from oobash filename ?
 
# 0.39.6
# -analyze log4oobash.properties
# -some System functions
# -colours ?

# 0.39.7
# -Man.generator: bold methodnames
# -better documentation for generators
# -add static FileUtils
# -chained return values

# 0.40
# Assert 
# + Tests (execute modules to test -- python style)

# 0.50
# Logging (coproc ?)

# 0.60
# Use of /dev/tcp and /dev/udp
# for network sockets

# 0.70
# -pimp getopts/or create an alternative ? 
# -oobash.freeze --> conservation of the status quo
# -oobash.onlineUpdate

# 0.80
# Facade for some version control systems ?

# 0.90 
# integration of 
# -jmxterm http://wiki.cyclopsgroup.org/jmxterm ? 
# -XMLStarlet http://xmlstar.sourceforge.net/ ?
# -xslt with http://xml.apache.org/xalan-j/commandline.html ?

# 1.0
# Facade for some GUI libs ?
# HAL 9000 integration
