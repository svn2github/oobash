# MAYBE ROADMAP (very unstable ;-) ):

# 0.39.3

look for other places in code for chained returnValues + Exception to stderr

Script.generator (header...)
Example: http://code.activestate.com/recipes/577862-bash-script-to-create-a-header-for-bash-scripts/?in=lang-bash

better documentation of generators in the oobash man page / Readme ? yes
documentation of  decorators ? yes

All constructors:
-----------------
constructors should throw IllegalArgumentExceptions instead of "echo ...; return 1" ???

try to care for http://developer.apple.com/library/mac/#documentation/Darwin/Reference/ManPages/
Process: ? yes
--------

see stacktrace : if os = Linux use proc filesystem, else ps and tail
 
http://docs.oracle.com/javase/7/docs/api/java/lang/ProcessBuilder.html

getPid --Done -not ready -Docs missing
getParentPid --Done -not ready -Docs missing
getUser --Done -not ready -Docs missing
getStatus --Done -Docs missing
Process.create --Done -not ready -Docs missing
Process.exists --Done -not ready -Docs missing --> __exists ???
kill --Done -not ready -Docs missing
getPriority --Done -not ready -Docs missing
setPriority --Done -not ready -Docs missing
__isDaemon (no controlling terminal) -- Yes
__getThreads ??
__children ??
...
lot of getters (ps: set of STANDARD FORMAT SPECIFIERS + http://developer.apple.com/library/mac/#documentation/Darwin/Reference/ManPages/man1/ps.1.html Keywords)

-new NotRunningException -- YES

toString for Process

Private:
---------
analyze log4oobash.properties ???

System:
-------
-Property functions:

declare -A assoca
env | while IFS='=' read key val
do
   echo assoca[$key]=$val
   assoca[$key]=$val
done
declare -p assoca

-load functions
-environment functions
-identityHashCode function
-nanoTime (used in Runtime) function

File:
------
http://docs.oracle.com/javase/7/docs/api/
there should be a solution with stat:
-getFreeSpace ? 
-getTotalSpace ?

Declare return value:
----------------------
Example:
 (local priority="$3") 2>/dev/null ||  (return 1)
Looks like bullshit !
Check again !
grep -r "(return 1)" * | grep -v txt |grep -v svn |wc -l
13

Check for writeableArray:
---------------------------
check for r (readonly !)
declare -iauxtr q
s="$(declare -p q)"
t=${s/declare -/}
array--> first letter is 'a' (after declare -p --> lexicographical order)
writeable ' ' (=end of block) before r

same with associativeArry....

# 0.39.4
# -some ssh

# 0.39.5
# -chained return values, url decode/encode ?
# -pimp getopts ?

# 0.40
# Assert 
# + Tests (execute modules to test --- python style)

# 0.50
# Logging

# 0.60
# Use of /dev/tcp and /dev/udp
# for network sockets

# 0.70 
# Facade for some GUI libs ?

# 0.80
# Facade for some version control systems ?

# 0.90 
# integration of 
# -jmxterm http://wiki.cyclopsgroup.org/jmxterm ? 
# -XMLStarlet http://xmlstar.sourceforge.net/ ?
# -xslt with http://xml.apache.org/xalan-j/commandline.html ?

# 1.0
# HAL 9000 integration
